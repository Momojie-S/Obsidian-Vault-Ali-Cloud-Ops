# 服务器初始化

> ⚠️ 重要：Homebrew 等工具不允许使用 root 运行，必须先创建普通用户。

## 创建非 root 用户

```bash
# 1. 创建用户（无密码）
/usr/sbin/useradd -m -s /bin/bash momojie

# 2. 复制 SSH 证书（与 root 相同）
mkdir -p /home/momojie/.ssh
cp /root/.ssh/authorized_keys /home/momojie/.ssh/
chown -R momojie:momojie /home/momojie/.ssh
chmod 700 /home/momojie/.ssh
chmod 600 /home/momojie/.ssh/authorized_keys

# 3. 添加 sudo 权限
/usr/sbin/usermod -aG sudo momojie
```

### 验证

```bash
# 确认用户创建成功
id momojie

# 确认 SSH 配置正确
ls -la /home/momojie/.ssh/
```

## SSH 配置

确保 `/etc/ssh/sshd_config` 中禁用密码登录：

```
PasswordAuthentication no
PubkeyAuthentication yes
```

## 配置 sudo 免密

Homebrew 安装需要：

```bash
echo "momojie ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/momojie
chmod 440 /etc/sudoers.d/momojie
```

## 安装 Homebrew

```bash
# 以 momojie 用户登录，使用代理安装
su - momojie
export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 配置环境变量（安装脚本会提示）
echo >> ~/.bashrc
echo 'eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv bash)"' >> ~/.bashrc
source ~/.bashrc

# 验证
brew --version
```

> 💡 国内访问 GitHub 需要代理，确保 Clash 运行中（端口 7890）

## 安装 Oh My Zsh

```bash
# 以 momojie 用户登录
su - momojie

# 安装 zsh（如果未安装）
sudo apt-get install -y zsh

# 安装 Oh My Zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# 如果需要代理
proxy_on
```

### 配置代理别名

在 `~/.zshrc` 末尾添加：

```bash
# Proxy settings
alias proxy_on='export http_proxy=http://127.0.0.1:7890 https_proxy=http://127.0.0.1:7890 all_proxy=socks5://127.0.0.1:7890 HTTP_PROXY=http://127.0.0.1:7890 HTTPS_PROXY=http://127.0.0.1:7890 ALL_PROXY=socks5://127.0.0.1:7890'
alias proxy_off='unset http_proxy https_proxy all_proxy HTTP_PROXY HTTPS_PROXY ALL_PROXY'

# Function to check proxy status
proxy_status() {
    if [ -n "$http_proxy" ]; then
        echo "Proxy is ON: $http_proxy"
    else
        echo "Proxy is OFF"
    fi
}
```

使用方式：
- `proxy_on` - 开启代理
- `proxy_off` - 关闭代理
- `proxy_status` - 查看代理状态

## 安装 NVM

```bash
# 以 momojie 用户登录，开启代理
su - momojie
proxy_on

# 获取最新版本号
NVM_VERSION=$(curl -sI https://github.com/nvm-sh/nvm/releases/latest 2>/dev/null | grep -i location | sed 's/.*tag\///' | tr -d '\r')
echo "Latest NVM version: $NVM_VERSION"

# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/$NVM_VERSION/install.sh | bash

# 加载 nvm
source ~/.zshrc

# 安装 Node.js LTS 版本
nvm install --lts

# 验证
node -v
npm -v
```

## 配置 Vim

创建 `~/.vimrc` 文件：

```vim
" 自动缩进
set autoindent
set smartindent
set expandtab       " 将 Tab 展开为空格
set tabstop=2       " 显示一个 Tab 为 2 个空格宽度
set shiftwidth=2    " 自动缩进使用 2 个空格
set softtabstop=2   " 编辑时 Tab 键占用 2 个空格

" 启用语法高亮
syntax on
" 根据文件类型设置缩进和插件
filetype on          " 检测文件类型
filetype plugin on   " 加载文件类型插件
filetype indent on   " 根据文件类型自动缩进
```

## 安装 uv (Python 包管理器)

```bash
# 以 momojie 用户登录
su - momojie

# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# 加载环境
source ~/.zshrc

# 验证
uv --version
```

## 安装 pnpm

```bash
# 安装 pnpm（需要先安装 Node.js）
npm install -g pnpm

# 验证
pnpm --version
```
