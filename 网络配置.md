# 网络配置

## DNS over HTTPS (DoH)

### 配置时间
2026-02-26

### 配置内容

编辑 `/etc/systemd/resolved.conf`：

```ini
[Resolve]
DNS=223.5.5.5#dns.alidns.com 119.29.29.29#doh.pub
DNSOverTLS=yes
```

> 注：国外 DoH（Cloudflare/Google）在国内可能被干扰，换成阿里 DNS 和腾讯 DNS 更稳定。

重启服务：

```bash
sudo systemctl restart systemd-resolved
```

### 验证

```bash
resolvectl status
# 确认显示 +DNSOverTLS
```

### 说明

- DoH 将 DNS 查询封装为 HTTPS 请求，防止 ISP 窥探和 DNS 污染
- 可独立使用，不依赖代理
- 配合 redsocks + iptables 透明代理时，DNS 也会走代理

### 相关命令

```bash
# 查看 DNS 状态
resolvectl status

# 测试 DNS 解析
resolvectl query google.com

# 查看 DoH 连接
resolvectl statistics
```

---

## 透明代理（待配置）

计划使用 redsocks + iptables 实现全局透明代理，将所有 TCP 流量转发到 Clash（Docker 暴露的 SOCKS5 端口）。

### 相关文件
- Clash Docker 配置：`/root/docker/clash/`
- Clash SOCKS5 端口：7891
